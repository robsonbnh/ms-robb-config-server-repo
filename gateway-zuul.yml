server:
  port: 8765

zuul:
  routes:
    user:
      service-id: "user"
      path: "/user/**"
      sensitive-headers: Cookie,Set-Cookie
    oauth:
      service-id: "oauth"
      path: "/oauth/**"
      sensitive-headers: Cookie,Set-Cookie

hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 60000

ribbon:
  ConnectTimeout: 10000
  ReadTimeout: 20000