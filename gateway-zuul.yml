server:
  port: 8765


zuul:
  routes:
    user:
      service-id: "user"
      path: "/user/**"

hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 60000

ribbon:
  ConnectTimeout: 10000
  ReadTimeout: 20000