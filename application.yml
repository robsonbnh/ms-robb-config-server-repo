info:
  app:
    name: "@project.name@"
    description: "@project.description@"
    version: "@project.version@"

oauth:
  clientName: myappname123
  clientSecret: myappsecret123

jwt:
  secret: MY-JWT-SECRET

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: "*"
